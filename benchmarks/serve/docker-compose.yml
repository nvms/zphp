services:
  php-fpm:
    build:
      context: .
      dockerfile: Dockerfile.fpm
    ports:
      - "9081:80"
    platform: linux/amd64

  swoole:
    build:
      context: .
      dockerfile: Dockerfile.swoole
    ports:
      - "9082:9501"
    platform: linux/amd64
